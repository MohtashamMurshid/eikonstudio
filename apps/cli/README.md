# @eikon/cli

CLI workspace package scaffold for the upcoming OpenTUI interface.

## Runtime

This package is intended to run with Bun.

## Scripts

- `pnpm --filter @eikon/cli dev`
- `pnpm --filter @eikon/cli build`
- `pnpm --filter @eikon/cli typecheck`
