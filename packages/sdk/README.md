# @eikon/sdk

Shared TypeScript SDK for Eikon Studio APIs.

Initial scope:

- Typed `generateImage` client for `POST /api/v1/generate`

This package is intended to be consumed by the upcoming CLI and other automation tools.
